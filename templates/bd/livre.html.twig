{% extends 'base.html.twig' %}

{% block title %}Liste des auteurs{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
            display:flex;
            flex-direction:column;
            align-items:center;
			margin: 1em auto;
			max-width: 850px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
			background: rgba(75,93,104,0.3);
			border-radius: 10px;
			padding: 50px
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
        a{
          color: #3a3a3a;  
        }
	</style>

	<div class="example-wrapper my-5">
		{# <h1>Hello
			{{ controller_name }}! ✅</h1>

		This friendly message is coming from:
		<ul>
			<li>Your controller at
				<code>
					<a href="{{ 'C:/Users/brice/Documents/GitHub/blog_bd/src/Controller/BdController.php'|file_link(0) }}">src/Controller/BdController.php</a>
				</code>
			</li>
			<li>Your template at
				<code>
					<a href="{{ 'C:/Users/brice/Documents/GitHub/blog_bd/templates/bd/index.html.twig'|file_link(0) }}">templates/bd/index.html.twig</a>
				</code>
			</li>
		</ul> #}
		
    <h1 class="mb-5">{{ liste }}</h1>
	{% for auteur in auteurs %}
	<p><a href="{{ path('bd_show', {id :auteur.id}) }}">{{ auteur.auteur }}</a></p>
	{% endfor %}
	</div>
{% endblock %}
